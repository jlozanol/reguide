"""Load an ARTG export into data/reguide.db.

The TGA search tool exports results to CSV or Excel, one export per therapeutic
type. Drop the files in data/ and run this. Records the snapshot date so reports
can state how current the comparison is.
"""

if __name__ == "__main__":
    raise SystemExit("not implemented")
